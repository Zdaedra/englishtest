# Executive English — схема подписок (Free / Core / AI)

> ТЗ на упаковку **существующих** возможностей в 3 тарифа. Без добавления новых функций.
> Версия 1 · 2026-06-07 · автор: Claude (по запросу Алексея). Только спека — не код, не деплой.
> Связано: продуктовый разворот [[english-productization-pivot]] (B2C, freemium + платный AI).

---

## 0. Принципы

1. **Без новых функций.** Тарифы — это распределение того, что УЖЕ построено, + лимиты. Лимит/доступ — рычаг, не новая фича.
2. **Гейтинг по себестоимости + по ценности.** Дорогое нам (LLM-коуч, server-STT, TTS-аудио) — выше по тарифу. Плюс одна продуктовая линия: **голосовой ответ через микрофон — целиком AI**, даже бесплатный нам Web Speech, потому что «сказать вслух → получить оценку/разбор» и есть ядро AI-ценности (крючок). Просмотр, свайп, самопроверка по эталону, путь, статистика — во Free.
3. **Free — это воронка, а не демо-огрызок.** Достаточно, чтобы дойти до «aha» и захотеть больше; ограничения создают естественные триггеры апгрейда.
4. **Freemium-петля:** Free пробует → упирается в стену (лимит/AI-коуч) → Core/AI.

---

## 1. Карта себестоимости (что нам стоит денег)

| Возможность | Стоимость нам | Провайдер |
|---|---|---|
| AI Coach (разбор: feedback/better/tone) | **высокая** | LLM `gpt-4.1-mini` |
| Answer Check — server STT (запись→текст) | средняя | `gpt-4o-mini-transcribe` |
| Аудио: gapless-сессии + per-phrase TTS | средняя (≈3 МБ/мин, биллинг по символам) | OpenAI TTS |
| LLM-скоринг ответа (неоднозначные случаи) | низкая | `gpt-4.1-nano` |
| Распознавание речи через **Web Speech** (iOS/браузер) | **0** (на устройстве/Apple) | клиент |
| Локальный gate-скоринг (точное совпадение) | **0** | наш код |
| Просмотр библиотеки, свайп-практика, путь, статистика, адаптив | **0** | статика/наш код |
| Импорт батча | разовая (LLM-парс фолбэк + генерация обложки) | mixed |

**Вывод:** AI = микрофон (голосовой ответ) + AI-коуч; Core = безлимит навыков/аудио + импорт (без микрофона); Free = просмотр + свайп + самопроверка по эталону.

---

## 2. Три тарифа

| | **Free** | **Executive Core** (платный) | **Executive AI** (премиум) |
|---|---|---|---|
| Позиционирование | Распробовать присутствие в реальных сценах | Серьёзная самостоятельная практика | Персональный AI-коуч |
| Кому | Новый пользователь, воронка | Учится сам, без коуча | Хочет разбор и рост качества |
| `plan` | `free` | `core` | `ai` |

---

## 3. Матрица «возможность × тариф» (только существующее)

| Возможность (уже в продукте) | Free | Core | AI |
|---|:--:|:--:|:--:|
| Просмотр всей библиотеки (89 наборов, секции, обложки, темы) | ✓ | ✓ | ✓ |
| Активные навыки (активированные батчи одновременно) | до **3** | ∞ | ∞ |
| Свайп-практика (знаю/не знаю + раскрытие фразы) | ✓ | ✓ | ✓ |
| Путь обучения (карта) — по активным навыкам | ✓ | ✓ (полный) | ✓ |
| 3-урочный flow + экзамены | по активным, аудио лимит | ✓ полный | ✓ |
| **Голосовой ответ (микрофон)** — сказать фразу вслух | ✗ (микрофон залочен) | ✗ (микрофон залочен) | **✓** (Web Speech + server-STT) |
| Самопроверка по эталону (свайп «Знаю» → показ верной фразы) | ✓ | ✓ | ✓ |
| Answer Check — скоринг произнесённого | ✗ | ✗ | полный LLM-скоринг |
| Аудио: per-phrase тап | по активным навыкам | все, без лимита | все |
| Аудио: gapless-сессии (Active Recall / Listening) | короткий дневной лимит | ∞ (fair-use) | ∞ |
| **AI Coach** (разбор ответа: как прозвучал, сильнее, тон) | ✗ (тизер→апселл) | ✗ (тизер→апселл) | **✓** |
| Адаптив: статистика/mastery/итог сессии | ✓ | ✓ | ✓ |
| Импорт своих наборов | ✗ | ✓ | ✓ |
| Настройки Listening (голос/скорость/паузы) | ✓ | ✓ | ✓ |

---

## 4. Лимиты (рычаг экономики; числа — дефолт, утвердить)

| Лимит (в день, per-user) | Free | Core | AI |
|---|--:|--:|--:|
| Голосовых проверок со скорингом (микрофон) | 0 (микрофон залочен) | 0 (микрофон залочен) | 400 |
| AI Coach-разборов | 0 | 0 | 150 (fair-use) |
| Gapless-аудиосессий | 3 | ∞ | ∞ |
| Активных навыков (всего, не в день) | 3 | ∞ | ∞ |

- Текущий глобальный `_DAILY_CAP=400` → станет **per-plan** (см. §6).
- TTS-аудио — главный «тихий» расход Free: ограничиваем числом gapless-сессий/день (per-phrase тап по активным навыкам оставляем — он дешёвый и нужен для ценности).
- **Микрофон (голосовой ответ) — целиком AI** (и Web Speech, и server-STT). Free/Core свайпают «Знаю/Не знаю» и сверяются с эталоном глазами; произнести вслух и получить оценку/разбор — только в Executive AI. Это и есть AI-«крючок».

---

## 5. Триггеры апгрейда (где стена и апселл)

**Free → Core:**
- Активировать 4-й навык.
- Запустить gapless-сессию сверх дневного лимита.
- Импортировать свой набор.

**Free / Core → AI:**
- Свайп вправо «Знаю» → **залоченный микрофон** + тизер «✦ Ответить голосом и получить разбор — Executive AI →» (построено).
- Тап по **AI Coach** на result-грани карточки (тизер «✦ Разбор от AI-коуча — Executive AI →», построен).
- Карточка плана в кабинете «Перейти на Executive AI →» (построена).

Каждая стена — мягкая карточка-апселл (не модалка-блокер), ведёт в кабинет/checkout.

---

## 6. Модель и enforcement (что менять в реализации — НЕ сейчас)

- `User.plan`: расширить `free | core | ai` (сейчас `free | ai`).
- Единый источник прав: `entitlements(plan) -> {voice_answer, server_stt, ai_coach, import, scored_per_day, max_active_batches}`. Бэкенд читает его на каждом гейте. **(реализовано: `backend/app/entitlements.py`)**
- Точки проверки (существующие эндпоинты, реализовано): `/training/answer` и `/training/answer-text` (микрофон → `voice_answer`, т.е. `ai`; иначе 403 `ai_required`), `/training/coach` (`ai_coach` → `ai`), активация батча (`/progress` PUT → cap `max_active_batches` для Free → 403 `limit_active`), `/imports/*` (`import` → `core+` → 403 `core_required`), `_check_rate` (per-plan `scored_per_day`).
- `_check_rate` → берёт лимит из `entitlements(plan)`, а не константу.
- Фронт: апселл-карточки на стенах (часть уже есть), бейдж плана в кабинете.
- **Биллинг (Stripe)** ставит `plan` по факту оплаты (отдельное ТЗ; здесь не описывается).

---

## 7. Цены — решает основатель (не финсовет)

Структура (числа — иллюстрация, не рекомендация):
- **Free** — $0.
- **Core** — мес./год (год дешевле); «всё, кроме AI-коуча».
- **AI** — выше Core; «+ AI-коуч».
- Опц.: 7-дневный trial на AI; годовая скидка.

---

## 8. Открытые решения (твой выбор)

1. **Free active-skills cap** — 3 (предложено) / 1 секция / другое.
2. **Free дневные лимиты** — gapless-сессий/день; считаем ли Web-Speech-проверки (предложено: не лимитируем жёстко, т.к. нам 0, но мягкий потолок от абьюза).
3. **Free server-STT** — n/a: голос целиком AI-only (см. ниже).
4. **Core vs AI разрыв** — ✅ **решено (основатель):** весь голосовой ответ (микрофон: Web Speech + server-STT + скоринг + коуч) = **AI**. Core = безлимит навыков/аудио + импорт, но без микрофона. AI = микрофон + коуч.
5. **Цены и trial** — твои числа.

После твоих ответов обновлю §4/§7 и (если скажешь) распишу отдельное ТЗ на enforcement + Stripe.
